# Kubernetes manifests

Put your cluster manifests here when you are ready to move beyond Docker Compose.

Suggested starting point:

- `discord-hub/deployment.yaml`
- `discord-hub/service.yaml`
- `discord-hub/secret.yaml`
